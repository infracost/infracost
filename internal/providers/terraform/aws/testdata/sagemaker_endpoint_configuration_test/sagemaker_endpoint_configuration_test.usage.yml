version: 0.1
resource_usage:
  aws_sagemaker_endpoint_configuration.my_serverless_config:
    # Requests handled by standard on-demand scaling
    monthly_inference_duration_seconds: 500000 
    # Requests handled by provisioned "warm" slots
    monthly_pc_inference_duration_seconds: 500000
    # Total data (IN + OUT) in GB
    monthly_data_processed_gb: 20
